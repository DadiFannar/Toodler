import { StyleSheet } from "react-native";

export default StyleSheet.create({
    image: {
        borderRadius: 20,
        width:230,
        height:150,
        margin: 10,
        justifyContent: 'center',
    },
})
